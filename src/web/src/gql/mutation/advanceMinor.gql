mutation AdvanceMinor(
  $minorId: BigInt!
){
  advanceMinor(input: {
    minorId: $minorId
  }) {
    minor {
      id
      name
      release: releaseByReleaseId {
        id
        name
        number
        status
      }
    }
  }
}